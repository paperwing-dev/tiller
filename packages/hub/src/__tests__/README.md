# Frontend Tests

**This README is AI-generated.**

This folder contains Hub UI and browser-state tests.

## Purpose

Coverage includes:

- application and workspace routing
- environment, session, settings, setup, and update UI
- plan writer, reviewer, and implementation-review workflows
- live synchronization, terminal recovery, and browser terminal behavior
- GitHub repository and publication presentation

Tests for small state helpers and React components share this directory. The
Hub test setup lives in `src/test-setup.ts`.

Run the suite from the repository root with:

```bash
npm run test --workspace packages/hub
```
